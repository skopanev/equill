//! A store that names which types it embeds: what the filter does, what it
//! tells the operator, and what a configure that failed halfway leaves behind.
mod caps;
mod crash;
mod reports;
mod retry;
mod types;
